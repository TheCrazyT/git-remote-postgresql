name: Manual workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: "run test"
      run: cd ${GITHUB_WORKSPACE} && find / -name run_test.sh && bash ./.github/workflows/run_test.sh
